# 实施记录

- 选择了单文件原生 Canvas 实现，以严格满足“保存为 HTML 后直接在浏览器打开”的运行条件。
- 迷宫使用归一化矩形线墙，因此全屏缩放时仍能维持碰撞与布局比例。
- Web Audio API 仅在第一次用户点击后初始化，符合浏览器对音频手势解锁的限制。
- `?demo` 会启用自动定时声呐，供可视化验收时展示核心状态，无需自动操作真实游戏路径。
- 单文件内的断环标记具备内嵌 SVG 回退；即使脱离项目服务、直接以本地文件打开，核心玩法、图形和音效均不依赖外部库或图片。
- 关卡制采用固定的三扇区配置，而非随机怪物数量；每关独立定义墙体、出生点、出口、怪物数、墙体余像时间、声波速度和调查速度。
- 关卡间使用 Enter 或左键确认，避免玩家的连续移动意外进入下一关；失败的 R 键只重置当前关，最终完成后的 R 键才返回第一关。
- 可玩性迭代以“三格电荷 + 碎片解锁 + 警觉加速”构成风险回路。电荷限制连续扫描，碎片让出口具有局内目标，警觉读数让多次发声实际改变怪物的调查压力。
- 墙体继续只以回声切片出现；碎片和出口使用唯一的旧金色，资源 HUD 仅贴附在边缘，不覆盖移动空间。
- 章节重构把旧的三关资源递进降为参考，实现了四种遭遇情绪：观察巡逻、主动诱导、静默舱重置和限时追猎撤离。每章先给出可读简报，再把已学规则组合进章节反转。
- 追猎阶段每 520ms 统一更新所有怪物到玩家的最后位置；静默舱会让怪物放弃调查，确保追猎有可理解的处理方式而非无预兆惩罚。
- 开场不再直接进入章节一。序章状态流为：失落信号标题 → 设备苏醒 → 无威胁移动校准 → 主动首次回声 → 远处异常回应 → 第一章简报。该流程只在玩家首次主动发声后才显露威胁，避免解释性说明取代体验。
- `?demo` 保持直接进入章节一且关闭怪物碰撞，专用于稳定展示声呐、墙体和章节 HUD；常规访问完整运行序章。
- 设备苏醒会从三条短通讯中随机选择一条，以语音合成、带通噪声和低频声调模拟损坏记录；仅在用户主动接通设备后触发，避免浏览器自动播放限制。
- 序章完成解锁本地持久化的 AR-01《失落信号》。玩家可用 F 打开三页档案、方向键或按钮翻页、Esc/F/关闭键回到原状态；首次回声到章节 I 的交接使用 780ms 雪花扫描线和横向信号错位。
- 合成语音已移除。设备苏醒改用无语言的带通噪声、低频脉冲、短高频载波和切断式反向音；随机短讯息只显示在日志中。
- 信号完整度按每章的额外声呐次数和累计失败次数计算，范围为 35%–100%。完整度决定当章解锁档案的三段数据可恢复数；档案 AR-01 至 AR-05 分别随序章和四章结算解锁，并持久化在本地浏览器。
- 四章分别配置一次近距环境声线索：折返滴答、确认载波、提前敲击和上行调制音。它们首次进入触发区后只播放一次，并在日志留下短记录；不使用箭头、标签或地图为玩家导航。
- 章节回放从档案或 C 键进入，只允许已恢复章节。系统保存每章历史最佳完整度；第四章完成后用四章最佳平均值选择 ER-00、ER-01 或 ER-02，门槛不显示给玩家。
- 终局视觉不依赖外部资源：ER-00 为吞没中心点的垂直漂移噪线；ER-01 为白色断环外的红色异常回应；ER-02 为越过白色水面线的旧金上行轨迹。最终状态层只承载记录标题和冷静结论。
- 每章成功通过后保存无伤与低声呐勋记，回放列表显示已获得标记；环境声触发后写入本地证据状态，对应档案显示“已证实”，未接近触发区则显示“仍存疑”。
- 旧的单次蜂鸣与损坏通讯组合已替换为持续 Web Audio 混音：41Hz/62Hz 拍频作为低频电床，带通粉尘噪声随警觉与追猎前移，静默舱会压低威胁层。所有反馈保持非语言化，文本记录不再被音频“朗读”。
- 音频总线在玩家首次操作后创建，以避免浏览器自动播放限制；`M` 可持久化切换静音。声呐、墙体、怪物、档案、诱饵、环境证据、追猎与失败均连接到压缩后的统一总线。
- 每个当前章节怪物拥有独立的低频失谐振荡器和窄带噪声源，声像映射其相对左右位置，增益/带通频率映射距离；静默舱和暂停监听面板会将其压低。监听设置以 `Esc` 打开，主音量与低频载荷保存在本地浏览器。
- 威胁视觉由最近怪物距离、警觉和追猎共同计算：暗角只位于 Canvas 之上、HUD 之下；微震只在高威胁时启动，并尊重系统减少动态偏好。设置页采用冷白刻度，旧金仍只用于出口与远端目标。
- 怪物空间声按类型，而非仅按索引区分：折返体使用低频锯齿与缓慢回授，确认体使用更高的三角脉冲和密带通摩擦，收束体使用更重的低频拍频与不规则尘噪。所有类型仍随左右位置、距离、静默舱和追猎状态改变。
- 监听校准按“左 → 右 → 中央低频 → 确认”序列执行，并继承当前音量和低频偏好。静默舱进入只触发一次归零：怪物声源淡出、暗角归零、冷白断续环扩张；离开安全区后重新武装。
- 本地双人原型保留 P1 为既有主声呐玩家（WASD + E/左键），新增 P2 为解析者（方向键 + /）。协作模式中只有 P2 的短程无引怪解析脉冲可恢复记录，出口需两人同在，主声呐仍承担暴露风险。
- P1/P2 以断环白点和方向短线区分；任一人倒地会进入 6.8 秒重连窗口，伙伴邻近按 R 可重连，双人倒地或窗口关闭才结束章节。进入前必须先选单人或双人信道，不再通过标题页数字键直接进入。
- 双人章节恢复记录后会出现 P1/P2 分站触点；两人必须同时驻留 900ms 才会开启撤离门。该机制不使用新色彩：触点与协作勋记均使用冷白，旧金仍专用于出口。
- 协作完成会保存“无断线”和“低警觉”勋记，并在关间结算和回放列表展示。两人相距超过短边 34% 时，屏幕边缘以中心点/断环/短线组合指向伙伴，同时用偏向该侧的低频双击保持联络；不会揭示路线或敌人。
- 双人第四章结算改读 CR-00《失配记录》、CR-01《双声残响》或 CR-02《同相离岸》，依据四章累计的无断线/低警觉勋记数量选择；阈值不显示为分数。三种终局分别采用分离断环、交错双环与并行旧金上行轨迹，避免复用单人 ER 视觉。
- 分站触点已按章节改为：I 同步驻留、II P1 先行后 P2 在 1.2 秒内响应、III P2 先行后 P1 在 0.9 秒内响应、IV 同步后共同释放并在 0.8 秒内重新同步。失步只回到冷白校验状态，不会额外生成敌人或警觉。
- 每章配置一个无视觉标记的伪回声位置。当两人处于远距分离且任一人未在静默舱时，它以一侧低频/带通纹理播放一次，并记录“远距回声 // 未校验”。它不影响怪物目标、警觉、路线或记录恢复，因此只能通过两名玩家沟通判断。
- 验证新增 `?demo&coop&trial=2`、`?demo&coop&decoy` 以及 `?demo&ending=CR-00|CR-01|CR-02` 预览入口。内联 Canvas 脚本使用 `scripts/check-echo-syntax.mjs` 解析检查，补足了 Vite/TypeScript 不会解析静态 HTML 内联脚本的验证缺口。
- 首屏改为单回路 / 双信道接入选择：`← / →` 切换、`1 / 2` 直选、`Enter` 确认，点击左右半屏则直接接通。选择以两枚右上缺口断环、中心白点、短线与 `SOURCE / SYNC / ARRAY` 冷白读数表示；单人进入序章苏醒，双人进入职责校准简报后开始第一章。
- 信道确认后进入 980ms 接通校验：选中断环的虚线弧和三枚短线向中心收束，再转入单人苏醒或双人职责简报。动画遵从减少动态偏好，并且只在用户确认后开始。
- 双人续行独立保存在 `sonar-coop-progress`：关间记录下一待玩章节，第四章完成时重置到第一章并递增完成次数；它不改变单人章节、档案、最佳完整度或最终记录。
- 按键映射存于 `sonar-key-bindings`，分单回路与双信道两组。设置内按动作捕获新键、同一模式冲突时交换原键；`Esc`、`M`、`F`、`C`、`Enter`、`1/2` 均作为系统键保留。移动、声呐、诱饵和双人重连/重载读取该映射；鼠标左键仍作为 P1 声呐的固定备用输入。
