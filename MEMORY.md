# 实施记录

- 选择了单文件原生 Canvas 实现，以严格满足“保存为 HTML 后直接在浏览器打开”的运行条件。
- 迷宫使用归一化矩形线墙，因此全屏缩放时仍能维持碰撞与布局比例。
- Web Audio API 仅在第一次用户点击后初始化，符合浏览器对音频手势解锁的限制。
- `?demo` 会启用自动定时声呐，供可视化验收时展示核心状态，无需自动操作真实游戏路径。
- 单文件内的断环标记具备内嵌 SVG 回退；即使脱离项目服务、直接以本地文件打开，核心玩法、图形和音效均不依赖外部库或图片。
- 关卡制采用固定的三扇区配置，而非随机怪物数量；每关独立定义墙体、出生点、出口、怪物数、墙体余像时间、声波速度和调查速度。
- 关卡间使用 Enter 或左键确认，避免玩家的连续移动意外进入下一关；失败的 R 键只重置当前关，最终完成后的 R 键才返回第一关。
- 可玩性迭代以“三格电荷 + 碎片解锁 + 警觉加速”构成风险回路。电荷限制连续扫描，碎片让出口具有局内目标，警觉读数让多次发声实际改变怪物的调查压力。
- 墙体继续只以回声切片出现；碎片和出口使用唯一的旧金色，资源 HUD 仅贴附在边缘，不覆盖移动空间。
