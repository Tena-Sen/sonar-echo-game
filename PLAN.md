# Game Plan: 《回声》 Sonar

## Risk Tasks

### 1. 声呐边缘扫描与短暂显形
- **Why isolated:** 声波需要在一次扩张中准确触发墙壁、怪物和终点，同时避免每帧重复播放回响音。
- **Approach:** 以玩家位置生成带唯一 ID 的圆形脉冲；对每个墙体记录该脉冲是否已经扫到，并以统一的 1.5 秒可见时间戳驱动淡出。
- **Verify:** 鼠标点击后出现单圈由内向外扩张的白环；被扫到的墙体、怪物与终点短暂亮起并在约 1.5 秒内退回黑暗；同一面墙对同一脉冲只触发一次回响。

### 2. 怪物的噪音响应与墙体碰撞
- **Why isolated:** 直线追踪容易穿墙或停在碰撞边缘，且追踪状态需要在抵达噪音源后可靠结束。
- **Approach:** 每个怪物接收声波命中时记录噪音源；移动按 X/Y 分轴尝试，沿迷宫矩形墙体滑动；抵达后保持两秒搜索态，再回到静止态。
- **Verify:** 声呐扫到怪物后，怪物向当次发声位置移动但不会穿过墙；抵达位置后维持短暂搜索，随后静止；触碰玩家立即进入失败状态。

## Main Build

构建一个只依赖 Canvas、原生 JavaScript 与 Web Audio API 的单文件全屏游戏。玩家可通过 WASD 或方向键移动，左键发射声呐；隐藏迷宫、怪物与出口仅会被回声短暂照亮；右上角常驻显示声呐使用次数。

- **Assets needed:** 声呐视觉参考图、透明标记、墙体余辉纹理参考与怪物轮廓参考。核心迷宫、波纹、怪物和光点由 Canvas 实时绘制，保证单文件可运行。
- **Verify:**
  - WASD 和方向键均可移动，玩家不会穿过迷宫墙壁。
  - 单击会发射声呐、增加计数并触发低沉脉冲；角色出现短促暗红暴露微光。
  - 迷宫默认不可见；墙、怪物与终点的显形颜色与时长符合对应语义。
  - 抵达出口显示胜利；被怪物触碰显示“你被吞噬了”；R 键可重置。
  - HUD 在窄屏与宽屏均保持可读，不遮挡主要游戏区域。
  - 运行期间无控制台错误；黑、白、余烬红、旧金的色彩密度与 `ASSETS.md` 视觉目标一致。

